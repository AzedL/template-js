{
  "name": "{{name}}",
  "description": "{{description}}",
  "author": "Azed",
  "private": true,
  "license": "MIT",
  "scripts": {
    "azg": "git init && az-git -n={{name}} -d=\"{{description}}\"",
    "plop": "plop",
    "test": "jest"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.16.11",
    "@types/jest": "^27.4.1",
    "jest": "^27.5.1",
    "plop": "^2.7.4"
  }
}
